mongo:
  image: 'mongo:3.0'
  volumes:
    - /data/db/mongo
  command: mongod --smallfiles
  ports:
    - '${MONGODB_PORT}:27017'
redis:
  image: 'redis:alpine3.10'
  volumes:
    - /data/db/redis
  ports:
    - '${REDIS_PORT}:6379'
